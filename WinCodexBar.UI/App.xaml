<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="WinCodexBar.UI.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinCodexBar.UI">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>
            <Color x:Key="AppAccentColor">#3B82F6</Color>
            <SolidColorBrush x:Key="AppAccentBrush" Color="{StaticResource AppAccentColor}"/>
            <SolidColorBrush x:Key="AppAccentMutedBrush" Color="#1F6FEB"/>
            <LinearGradientBrush x:Key="AppHeaderBackdropBrush" StartPoint="0,0" EndPoint="1,1" Opacity="0.10">
                <GradientStop Color="#0F1E2E" Offset="0"/>
                <GradientStop Color="#14283D" Offset="0.5"/>
                <GradientStop Color="#182B44" Offset="1"/>
            </LinearGradientBrush>
            <SolidColorBrush x:Key="AppCardBackgroundBrush"
                             Color="{ThemeResource CardBackgroundFillColorDefault}"
                             Opacity="0.88"/>
            <SolidColorBrush x:Key="AppCardStrokeBrush"
                             Color="{ThemeResource CardStrokeColorDefault}"
                             Opacity="0.55"/>
            <SolidColorBrush x:Key="AppGhostButtonBackgroundBrush"
                             Color="{ThemeResource ControlFillColorDefault}"
                             Opacity="0.35"/>
            <SolidColorBrush x:Key="AppGhostButtonBorderBrush"
                             Color="{ThemeResource ControlStrokeColorDefault}"
                             Opacity="0.45"/>
            <SolidColorBrush x:Key="TrayMenuBackgroundBrush"
                             Color="{ThemeResource CardBackgroundFillColorDefault}"
                             Opacity="0.97"/>
            <SolidColorBrush x:Key="TrayMenuBorderBrush"
                             Color="{ThemeResource CardStrokeColorDefault}"
                             Opacity="0.85"/>
            <SolidColorBrush x:Key="TrayMenuSeparatorBrush"
                             Color="{ThemeResource DividerStrokeColorDefault}"
                             Opacity="0.7"/>
            <SolidColorBrush x:Key="TrayMenuMeterTrackBrush"
                             Color="{ThemeResource ControlStrongFillColorDefault}"
                             Opacity="0.4"/>

            <Style x:Key="AppTitleTextStyle" TargetType="TextBlock" BasedOn="{StaticResource TitleTextBlockStyle}">
                <Setter Property="FontSize" Value="24"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="LineHeight" Value="28"/>
            </Style>
            <Style x:Key="AppSectionHeaderStyle" TargetType="TextBlock" BasedOn="{StaticResource SubtitleTextBlockStyle}">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
            </Style>
            <Style x:Key="AppPrimarySubtitleTextStyle" TargetType="TextBlock" BasedOn="{StaticResource SubtitleTextBlockStyle}">
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
            </Style>
            <Style x:Key="AppSubtleTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
            </Style>
            <Style x:Key="AppCardStyle" TargetType="Border">
                <Setter Property="Padding" Value="16"/>
                <Setter Property="CornerRadius" Value="12"/>
                <Setter Property="Background" Value="{StaticResource AppCardBackgroundBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource AppCardStrokeBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>
            <Style x:Key="AppHeaderCardStyle" TargetType="Border">
                <Setter Property="Padding" Value="16"/>
                <Setter Property="CornerRadius" Value="14"/>
                <Setter Property="Background" Value="{StaticResource AppHeaderBackdropBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>
            <Style x:Key="AppPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="12,6"/>
            </Style>
            <Style x:Key="AppGhostButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="Background" Value="{StaticResource AppGhostButtonBackgroundBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource AppGhostButtonBorderBrush}"/>
            </Style>
            <Style x:Key="AppProgressStyle" TargetType="ProgressBar">
                <Setter Property="Foreground" Value="{StaticResource AppAccentBrush}"/>
                <Setter Property="Background" Value="{ThemeResource ControlStrongFillColorDefaultBrush}"/>
                <Setter Property="Height" Value="8"/>
            </Style>
            <Style x:Key="TrayMenuCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource TrayMenuBackgroundBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource TrayMenuBorderBrush}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="CornerRadius" Value="0"/>
                <Setter Property="Padding" Value="0"/>
            </Style>
            <Style x:Key="TrayMenuInfoTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="LineHeight" Value="16"/>
            </Style>
            <Style x:Key="TrayMenuProviderTitleTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="LineHeight" Value="16"/>
            </Style>
            <Style x:Key="TrayMenuMetricLabelTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="LineHeight" Value="14"/>
            </Style>
            <Style x:Key="TrayMenuMetricPercentTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="LineHeight" Value="14"/>
            </Style>
            <Style x:Key="TrayMenuProgressBarStyle" TargetType="ProgressBar">
                <Setter Property="Minimum" Value="0"/>
                <Setter Property="Maximum" Value="100"/>
                <Setter Property="Height" Value="4"/>
                <Setter Property="Foreground" Value="{StaticResource AppAccentBrush}"/>
                <Setter Property="Background" Value="{StaticResource TrayMenuMeterTrackBrush}"/>
                <Setter Property="CornerRadius" Value="2"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style x:Key="TrayMenuActionButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="Padding" Value="12,7"/>
                <Setter Property="CornerRadius" Value="0"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>

